services:
  gogogot:
    image: gogogot:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - GOGOGOT_DATA_DIR=/data
    volumes:
      - ./data:/data
      - ./work:/work
    command: ["--model=minimax"]
